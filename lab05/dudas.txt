void *memcpy(void *dest, const void * src, size_t n)
dest − This is pointer to the destination array where the content is to be copied, type-casted to a pointer of type void*.

src − This is pointer to the source of data to be copied, type-casted to a pointer of type void*.

n − This is the number of bytes to be copied.





ej1a) el stack sin nada que seria, next apuntando a null y el valor de value no importa ?
	PREGUNTAR SI EN EL EJ DE REVERSE, NO CONVIENE HACER TODO EN UN SOLO FOR
	done
	FALTA SACARLE LOS LEAKS

ej1b) done 
	FALTA SACARLE LOS LEAKS

ej2) done -->>> Falta probar el stack_to_array y poner los asersts
	CORREGI:(1)Cuando haces malloc para una estructura no hay que poner el puntero que la apunta sino struc y el nombre; ej malloc(sizeof(struct _s_stack));
		(2)Cuando haces el destroy, primero hay de liberar la memoria que pedise en calloc

ej3) FALTA


PARA QUE SIRVEN EL ESTERISCO EN DIFERENTES LUGARRES (son dif tipos de punteros)

